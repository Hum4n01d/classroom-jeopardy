extends layout

block content
    h1 Jeopardy Boards
    if not boards
        p No boards

    .boards
        for board in boards
            .wrap
                a(href=url_for('game.play', board_id=board.id))= board.title